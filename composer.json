{
    "name": "veresnikov/template",
    "type": "project",
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "server-start": [
            "Composer\\Config::disableProcessTimeout",
            "php -S 0.0.0.0:8000"
        ],
        "test": "phpunit src/Tests"
    },
    "config": {
        "disable-tls": true
    },
    "require-dev": {
        "phpunit/phpunit": "^9.5"
    }
}
